# ML dependencies — ColPali image embeddings + torch
# Kept separate from pyproject.toml to avoid bloating the main API container.
# Install only in the ingestion worker (see Phase 3 in BUILD_GUIDE.md).
#
# Install (CPU-only, fast download — good for local dev):
#   pip install -r requirements-ml.txt --extra-index-url https://download.pytorch.org/whl/cpu
#
# Install (GPU/CUDA 12.1 — for production GPU worker):
#   pip install -r requirements-ml.txt --extra-index-url https://download.pytorch.org/whl/cu121

--extra-index-url https://download.pytorch.org/whl/cpu

torch>=2.2.0
transformers>=4.40.0
colpali-engine>=0.3.0
